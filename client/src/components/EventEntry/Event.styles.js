import styled from 'styled-components';

const EventDetails = styled.div`
    p {
        margin: 0.5rem 0;
    }
`;

const EventWarning = styled.p`
    color: red;
    font-weight: bold;
`;

export { EventDetails, EventWarning };
